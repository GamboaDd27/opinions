<%include ./partials/header %>

 
 <div id="myDiv"><!-- Plotly chart will be drawn inside this DIV --></div>
  <script>
  var a=<%=a%>;
  var b=<%=b%>;
  var c=<%=c%>;
    var data = [{

  y: [a, b, c],
  type: 'bar'
}];

Plotly.newPlot('myDiv', data);
  </script>
  <div class="ui right aligned grid">
  <div class="five wide column"><%=found.optiona%></div>
  <div class="five wide column"><%=found.optionb%></div>
  <div class="five wide column"><%=found.optionc%></div>
  </div>
<div class="ui huge centered header"><%=found.question%></div>
<%include ./partials/footer %>